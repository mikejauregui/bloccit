<h1> New Question</h1>
<div class="row">
  <div class="col-md-4">
    <p>Guidelines for questions</p>
    <ul>
      <li>Make it clear.</li>
      <li>Be consistent and go to the point.</li>
      <li>There are no stupid questions.</li>
    </ul>
  </div>
  <div class="col-md-8">
      <%= form_for @question, url:question_index_path do |form| %>
        <div class="form-group">
          <%= form.label :title%>
          <%= form.text_field :title, class: 'form-control', placeholder: "Enter question title"%>
        </div>
        <div class="form-group">
          <%= form.label :body%>
          <%= form.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter question body"%>
        </div>
        <div class="form-group">
          <%= form.submit "Save", class: 'btn btn-success' %>
        </div>
    <% end %>
  </div>
</div>
